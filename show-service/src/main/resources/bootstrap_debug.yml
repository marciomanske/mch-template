spring:
  data:
    mongodb:
      host: localhost
      username: user
      password: dminc
      database: dmi
      port: 25000
  application:
    name: show-service

server:
  context-path: /shows_test
  port: 5112
